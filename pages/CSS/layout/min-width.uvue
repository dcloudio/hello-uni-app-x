<template>
  <!-- #ifdef APP -->
  <scroll-view>
  <!-- #endif -->
    <view style="flex-grow: 1;">
      <view>
        <text>min-width: 250px</text>
        <view class="common" style="min-width: 250px;">
          <text>width: 50px</text>
        </view>
        <text>拍平测试</text>
        <view class="common" style="min-width: 250px;" flatten>
          <text>width: 50px</text>
        </view>
      </view>

      <view class="uni-common-mt">
        <text class="uni-title-text">setProperty 设置与 getPropertyValue 获取 min-width 测试</text>
      </view>

      <!-- 普通版本 -->
      <view class="test-container">
        <view class="test-item">
          <text class="uni-subtitle-text">view 组件</text>
          <text class="test-info">设置值: {{minWidth}}</text>
          <text class="test-info">获取值: {{minWidthActual}}</text>
          <view class="test-box">
            <view ref="viewRef" class="common" :style="{ minWidth: minWidth }">
              <text>view测试</text>
            </view>
          </view>
        </view>

        <view class="test-item">
          <text class="uni-subtitle-text">text 组件</text>
          <text class="test-info">设置值: {{minWidth}}</text>
          <text class="test-info">获取值: {{minWidthActualText}}</text>
          <view class="test-box">
            <text ref="textRef" class="common" :style="{ minWidth: minWidth }">text测试</text>
          </view>
        </view>

        <view class="test-item">
          <text class="uni-subtitle-text">image 组件</text>
          <text class="test-info">设置值: {{minWidth}}</text>
          <text class="test-info">获取值: {{minWidthActualImage}}</text>
          <view class="test-box">
            <image ref="imageRef" class="common" :style="{ minWidth: minWidth }" src="/static/logo.png"></image>
          </view>
        </view>
      </view>

      <!-- 拍平版本 -->
      <view class="test-container">
        <view class="test-item">
          <text class="uni-subtitle-text">view 组件拍平</text>
          <text class="test-info">设置值: {{minWidth}}</text>
          <text class="test-info">获取值: {{minWidthActualFlat}}</text>
          <view class="test-box">
            <view ref="viewRefFlat" class="common" :style="{ minWidth: minWidth }" flatten>
              <text>view测试</text>
            </view>
          </view>
        </view>

        <view class="test-item">
          <text class="uni-subtitle-text">text 组件拍平</text>
          <text class="test-info">设置值: {{minWidth}}</text>
          <text class="test-info">获取值: {{minWidthActualTextFlat}}</text>
          <view class="test-box">
            <text ref="textRefFlat" class="common" :style="{ minWidth: minWidth }" flatten>text测试</text>
          </view>
        </view>

        <view class="test-item">
          <text class="uni-subtitle-text">image 组件拍平</text>
          <text class="test-info">设置值: {{minWidth}}</text>
          <text class="test-info">获取值: {{minWidthActualImageFlat}}</text>
          <view class="test-box">
            <image ref="imageRefFlat" class="common" :style="{ minWidth: minWidth }" flatten src="/static/logo.png"></image>
          </view>
        </view>
      </view>

      <view class="uni-common-mt uni-common-mb">
        <text class="uni-tips">第一个枚举值，'' (空字符串) - 测试空值情况</text>
        <enum-data :items="minWidthEnum" title="min-width 枚举值" @change="radioChangeMinWidth" :compact="true"></enum-data>
        <input-data :defaultValue="minWidth" title="min-width 自定义值" type="text" @confirm="inputChangeMinWidth"></input-data>
      </view>
    </view>
  <!-- #ifdef APP -->
  </scroll-view>
  <!-- #endif -->
</template>

<script setup lang="uts">
  import { ItemType } from '@/components/enum-data/enum-data-types'

  const minWidthEnum: ItemType[] = [
    { value: 0, name: '' },
    { value: 1, name: '0' },
    { value: 2, name: '0px' },
    { value: 3, name: '50px' },
    { value: 4, name: '150px' },
    { value: 5, name: '0%' },
    { value: 6, name: '50%' }
  ]

  const minWidth = ref('100px')
  const minWidthActual = ref('')
  const minWidthActualText = ref('')
  const minWidthActualImage = ref('')
  const minWidthActualFlat = ref('')
  const minWidthActualTextFlat = ref('')
  const minWidthActualImageFlat = ref('')
  const viewRef = ref(null as UniElement | null)
  const textRef = ref(null as UniTextElement | null)
  const imageRef = ref(null as UniImageElement | null)
  const viewRefFlat = ref(null as UniElement | null)
  const textRefFlat = ref(null as UniTextElement | null)
  const imageRefFlat = ref(null as UniImageElement | null)

  const changeMinWidth = (value: string) => {
    minWidth.value = value
    // view 元素 - 普通版本
    viewRef.value?.style.setProperty('min-width', value)
    const actualValue = viewRef.value?.style.getPropertyValue('min-width')
    minWidthActual.value = actualValue ?? ''
    // view 元素 - 拍平版本
    viewRefFlat.value?.style.setProperty('min-width', value)
    const actualValueFlat = viewRefFlat.value?.style.getPropertyValue('min-width')
    minWidthActualFlat.value = actualValueFlat ?? ''
    // text 元素 - 普通版本
    textRef.value?.style.setProperty('min-width', value)
    const actualValueText = textRef.value?.style.getPropertyValue('min-width')
    minWidthActualText.value = actualValueText ?? ''
    // text 元素 - 拍平版本
    textRefFlat.value?.style.setProperty('min-width', value)
    const actualValueTextFlat = textRefFlat.value?.style.getPropertyValue('min-width')
    minWidthActualTextFlat.value = actualValueTextFlat ?? ''
    // image 元素 - 普通版本
    imageRef.value?.style.setProperty('min-width', value)
    const actualValueImage = imageRef.value?.style.getPropertyValue('min-width')
    minWidthActualImage.value = actualValueImage ?? ''
    // image 元素 - 拍平版本
    imageRefFlat.value?.style.setProperty('min-width', value)
    const actualValueImageFlat = imageRefFlat.value?.style.getPropertyValue('min-width')
    minWidthActualImageFlat.value = actualValueImageFlat ?? ''
  }

  const radioChangeMinWidth = (index: number) => {
    const selectedItem = minWidthEnum.find((item): boolean => item.value === index)
    if (selectedItem != null) {
      changeMinWidth(selectedItem.name)
    }
  }

  const inputChangeMinWidth = (value: string) => {
    changeMinWidth(value)
  }

  onReady(() => {
    // view 元素 - 普通版本
    const actualValue = viewRef.value?.style.getPropertyValue('min-width')
    minWidthActual.value = actualValue ?? ''
    // view 元素 - 拍平版本
    const actualValueFlat = viewRefFlat.value?.style.getPropertyValue('min-width')
    minWidthActualFlat.value = actualValueFlat ?? ''
    // text 元素 - 普通版本
    const actualValueText = textRef.value?.style.getPropertyValue('min-width')
    minWidthActualText.value = actualValueText ?? ''
    // text 元素 - 拍平版本
    const actualValueTextFlat = textRefFlat.value?.style.getPropertyValue('min-width')
    minWidthActualTextFlat.value = actualValueTextFlat ?? ''
    // image 元素 - 普通版本
    const actualValueImage = imageRef.value?.style.getPropertyValue('min-width')
    minWidthActualImage.value = actualValueImage ?? ''
    // image 元素 - 拍平版本
    const actualValueImageFlat = imageRefFlat.value?.style.getPropertyValue('min-width')
    minWidthActualImageFlat.value = actualValueImageFlat ?? ''
  })
</script>

<style>
  .common {
    width: 50px;
    height: 80px;
    background-color: red;
    justify-content: center;
    align-items: center;
  }

  .test-container {
    flex-direction: row;
    justify-content: space-between;
    margin-top: 10px;
  }

  .test-item {
    flex: 1;
    margin: 0 5px;
  }

  .test-box {
    width: 100%;
    height: 80px;
    background-color: gray;
  }

  .test-info {
    font-size: 12px;
    color: #666;
  }
</style>
