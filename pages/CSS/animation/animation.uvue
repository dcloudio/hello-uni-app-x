<template>
	<view class="page">
		<text class="title">@keyframes/animation 动画示例</text>
		<text class="desc">下方两个方块分别演示位移缩放动画和脉冲动画。</text>

		<view class="demo">
			<text class="label">浮动方块</text>
			<view class="stage">
				<view class="demo-box demo-box-float"></view>
			</view>
		</view>

		<view class="demo">
			<text class="label">脉冲圆点</text>
			<view class="stage stage-center">
				<view class="pulse-wrap">
					<view class="pulse-ring"></view>
					<view class="pulse-core"></view>
				</view>
			</view>
		</view>
	</view>
</template>

<style>
	.page {
		padding: 16px;
	}

	.title {
		font-size: 20px;
		font-weight: 600;
		margin-bottom: 8px;
	}

	.desc {
		font-size: 14px;
		line-height: 20px;
		margin-bottom: 12px;
	}

	.demo {
		margin-bottom: 12px;
	}

	.label {
		font-size: 16px;
		font-weight: 600;
		margin-bottom: 8px;
	}

	.stage {
		display: flex;
		flex-direction: row;
		align-items: center;
		height: 180px;
		padding: 8px 0;
	}

	.stage-center {
		justify-content: center;
	}

	.demo-box {
		width: 72px;
		height: 72px;
		border-radius: 18px;
		background-color: #3b82f6;
	}

	.demo-box-float {
		animation: float-box 1.8s ease-in-out infinite alternate;
	}

	.pulse-wrap {
		position: relative;
		width: 96px;
		height: 96px;
	}

	.pulse-ring {
		position: absolute;
		left: 0;
		top: 0;
		width: 96px;
		height: 96px;
		border-radius: 48px;
		background-color: #93c5fd;
		opacity: 0.7;
		animation: pulse-ring-wave 2s ease-out infinite;
	}

	.pulse-core {
		position: absolute;
		left: 32px;
		top: 32px;
		width: 32px;
		height: 32px;
		border-radius: 16px;
		background-color: #2563eb;
		animation: pulse-core-beat 1.2s ease-in-out infinite;
	}

	@keyframes float-box {
		0% {
			transform: translateX(0px) translateY(0px) scale(1);
			background-color: #3b82f6;
		}

		100% {
			transform: translateX(180px) translateY(-12px) scale(1.12);
			background-color: #8b5cf6;
		}
	}

	@keyframes pulse-ring-wave {
		0% {
			transform: scale(0.4);
			opacity: 0.9;
		}

		100% {
			transform: scale(1);
			opacity: 0;
		}
	}

	@keyframes pulse-core-beat {
		0% {
			transform: scale(0.92);
		}

		50% {
			transform: scale(1.08);
		}

		100% {
			transform: scale(0.92);
		}
	}
</style>
