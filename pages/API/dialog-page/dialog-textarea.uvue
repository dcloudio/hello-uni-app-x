<template>
  <view class="dialog-container uni-theme-root">
    <view class="dialog-content">
      <button class="mt-10" id="dialog1-close-dialog" @click="closeDialog">
        closeDialog
      </button>
      <textarea class="dialog-textarea" placeholder-class="dialog-placeholder" placeholder="输入框..."></textarea>
    </view>
  </view>
</template>

<script setup lang="uts">
function closeDialog() {
  uni.closeDialogPage({
    success(res) {
      console.log("closeDialog success", res);
    },
    fail(err) {
      console.log("closeDialog fail", err);
    },
    complete(res) {
      console.log("closeDialog complete", res);
    },
  });
}
</script>

<style>
.dialog-container {
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.3);
  display: flex;
  justify-content: center;
  align-items: center;
}
.dialog-content {
  width: 90%;
  height: 80%;
  padding: 10px;
  background-color: var(--list-background-color, #fff);
  border-radius: 6px;
  position: relative;
}
.dialog-textarea {
  position: absolute;
  border: 1px solid #000;
  background-color: #ffffff;
  color: #333333;
  left: 10px;
  right: 10px;
  bottom: 10px;
}

.dialog-placeholder {
  color: #808080;
}

@media (prefers-color-scheme: dark) {
  .dialog-textarea {
    border-color: #666666;
    background-color: #3b3b3b;
    color: #ffffff;
  }

  .dialog-placeholder {
    color: #b8b8b8;
  }
}
</style>
